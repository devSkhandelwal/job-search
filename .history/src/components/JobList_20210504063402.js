import React from 'react'

const JobList = ({jobs}) => {
    return (
        <ul>
            {
                jobs.map(job=>(
                    
                ))
            }
        </ul>
    )
}

export default JobList
